name: 🧪 Testar Todos Exercícios
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Compilar e Rodar Exercícios
      run: |
        for dir in exercicio*/; do
          echo "🧪 Testando $dir"
          cd $dir
          cmake . && make
          ./main
          cd ..
        done
